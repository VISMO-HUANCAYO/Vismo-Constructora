(()=>{
const $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
const COMPANY='vismoCompanyV20', PROS='vismoProfessionalsV20', TPL='vismoInterviewTemplatesV20', INTERNS='vismoInternsV20';
const companyDefault={legal:'Corporación Visión & Modernidad “VISMOCORP”',trade:'VISMO',ruc:'20607812803',address:'Av. Valle Real Mz. B Lote 1, Urb. Valle Real, Centro Poblado La Punta, Sapallanga, Huancayo, Junín',rep:'',repDni:''};
let company={...companyDefault,...JSON.parse(localStorage.getItem(COMPANY)||'{}')};
let pros=JSON.parse(localStorage.getItem(PROS)||'[]'), templates=JSON.parse(localStorage.getItem(TPL)||'[]'), interns=JSON.parse(localStorage.getItem(INTERNS)||'[]');
function loadCompany(){companyLegal.value=company.legal;companyTrade.value=company.trade;companyRuc.value=company.ruc;companyAddress.value=company.address;companyRep.value=company.rep;companyRepDni.value=company.repDni}
companySave?.addEventListener('click',()=>{company={legal:companyLegal.value.trim(),trade:companyTrade.value.trim(),ruc:companyRuc.value.trim(),address:companyAddress.value.trim(),rep:companyRep.value.trim(),repDni:companyRepDni.value.trim()};localStorage.setItem(COMPANY,JSON.stringify(company));alert('Datos de la empresa guardados.')});
function renderPros(){proList.innerHTML=pros.length?pros.map((x,i)=>`<div class="listitem"><div><b>${esc(x.prefix)} ${esc(x.name)}</b><br><small>${esc(x.role||'')} ${x.college?'· '+esc(x.college):''}</small></div><div>${esc(x.dni||'')}</div><div></div><div><button class="btn tiny proDel" data-i="${i}">Eliminar</button></div></div>`).join(''):'<p class="hint">Aún no hay profesionales registrados.</p>';internSupervisor.innerHTML='<option value="">Sin supervisor seleccionado</option>'+pros.map((x,i)=>`<option value="${i}">${esc(x.prefix)} ${esc(x.name)}</option>`).join('');document.querySelectorAll('.proDel').forEach(b=>b.onclick=()=>{pros.splice(+b.dataset.i,1);localStorage.setItem(PROS,JSON.stringify(pros));renderPros()})}
proAdd?.addEventListener('click',()=>{if(!proName.value.trim())return alert('Escribe el nombre del profesional.');pros.push({prefix:proPrefix.value.trim(),name:proName.value.trim(),dni:proDni.value.trim(),college:proCollege.value.trim(),role:proRole.value.trim()});localStorage.setItem(PROS,JSON.stringify(pros));['proPrefix','proName','proDni','proCollege','proRole'].forEach(id=>$(id).value='');renderPros()});
function addQ(q={text:'',type:'text',required:false}){let d=document.createElement('div');d.className='payrow tplQ';d.innerHTML=`<input class="qText" placeholder="Pregunta" value="${esc(q.text)}"><select class="qType"><option value="text">Texto corto</option><option value="textarea">Texto largo</option><option value="number">Número</option><option value="date">Fecha</option><option value="select">Selección</option><option value="multiselect">Selección múltiple</option><option value="file">Fotografía / archivo</option><option value="rooms">Ambientes por piso</option></select><label><input class="qReq" type="checkbox" ${q.required?'checked':''}> Obligatoria</label><button class="btn tiny" type="button" onclick="this.parentElement.remove()">×</button>`;d.querySelector('.qType').value=q.type;tplQuestions.appendChild(d)}
tplAddQuestion?.addEventListener('click',()=>addQ());
tplSave?.addEventListener('click',()=>{let title=tplTitle.value.trim();if(!title)return alert('Escribe un nombre para la plantilla.');let qs=[...document.querySelectorAll('.tplQ')].map(x=>({text:x.querySelector('.qText').value.trim(),type:x.querySelector('.qType').value,required:x.querySelector('.qReq').checked})).filter(x=>x.text);templates.push({id:'TPL-'+Date.now(),title,desc:tplDesc.value.trim(),questions:qs,created:new Date().toISOString()});localStorage.setItem(TPL,JSON.stringify(templates));tplTitle.value='';tplDesc.value='';tplQuestions.innerHTML='';renderTemplates();alert('Plantilla guardada.')});
function enc(o){return btoa(unescape(encodeURIComponent(JSON.stringify(o)))).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
function renderTemplates(){tplList.innerHTML=templates.length?templates.map((t,i)=>`<div class="listitem"><div><b>${esc(t.title)}</b><br><small>${esc(t.desc||'')}</small></div><div>${t.questions.length} pregunta(s)</div><div></div><div class="projectActions"><button class="btn tplLink" data-i="${i}">Generar enlace</button><button class="btn tplDup" data-i="${i}">Duplicar</button><button class="btn tplDel" data-i="${i}">Eliminar</button></div></div>`).join(''):'<div class="panel">Aún no hay plantillas personalizadas.</div>';document.querySelectorAll('.tplLink').forEach(b=>b.onclick=()=>{let t=templates[+b.dataset.i],url=new URL('../entrevista.html',location.href);url.searchParams.set('tpl',enc(t));prompt('Copia este enlace para el cliente:',url.href)});document.querySelectorAll('.tplDup').forEach(b=>b.onclick=()=>{let t=templates[+b.dataset.i];templates.push({...t,id:'TPL-'+Date.now(),title:t.title+' · copia'});localStorage.setItem(TPL,JSON.stringify(templates));renderTemplates()});document.querySelectorAll('.tplDel').forEach(b=>b.onclick=()=>{if(confirm('¿Eliminar esta plantilla?')){templates.splice(+b.dataset.i,1);localStorage.setItem(TPL,JSON.stringify(templates));renderTemplates()}})}
function internData(){return{name:internName.value.trim(),dni:internDni.value.trim(),career:internCareer.value.trim(),faculty:internFaculty.value.trim(),school:internSchool.value.trim(),start:internStart.value,end:internEnd.value,hours:internHours.value,supervisor:internSupervisor.value===''?null:pros[+internSupervisor.value]}}
internSave?.addEventListener('click',()=>{let x=internData();if(!x.name)return alert('Escribe el nombre del practicante.');interns.push({...x,id:'PRA-'+Date.now()});localStorage.setItem(INTERNS,JSON.stringify(interns));renderInterns();alert('Practicante guardado.')});
function renderInterns(){internList.innerHTML=interns.length?interns.map((x,i)=>`<div class="listitem"><div><b>${esc(x.name)}</b><br><small>${esc(x.career)} · ${esc(x.school)}</small></div><div>${esc(x.start)} → ${esc(x.end)}</div><div>${esc(x.hours)} h</div><div><button class="btn tiny internLoad" data-i="${i}">Cargar</button></div></div>`).join(''):'<p class="hint">Aún no hay practicantes guardados.</p>';document.querySelectorAll('.internLoad').forEach(b=>b.onclick=()=>{let x=interns[+b.dataset.i];internName.value=x.name;internDni.value=x.dni;internCareer.value=x.career;internFaculty.value=x.faculty;internSchool.value=x.school;internStart.value=x.start;internEnd.value=x.end;internHours.value=x.hours;let pi=pros.findIndex(p=>p.name===x.supervisor?.name);internSupervisor.value=pi>=0?String(pi):''})}
function fmtDate(v){if(!v)return '____';let [y,m,d]=v.split('-');return `${d}/${m}/${y}`}
internCertificate?.addEventListener('click',()=>{let x=internData();if(!x.name)return alert('Completa al menos el nombre del practicante.');let sup=x.supervisor;let supName=sup?`${esc(sup.prefix)} ${esc(sup.name)}`:(company.rep?esc(company.rep):'________________________');let supMeta=sup?`${sup.role?esc(sup.role):''}${sup.college?`<br>${esc(sup.college)}`:''}`:'';let today=new Date(),months=['enero','febrero','marzo','abril','mayo','junio','julio','agosto','septiembre','octubre','noviembre','diciembre'],issued=`${today.getDate()} de ${months[today.getMonth()]} de ${today.getFullYear()}`;let logo=new URL('logo-documento.png',location.href).href;let w=window.open('','_blank');if(!w)return alert('Permite ventanas emergentes para generar la constancia.');w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Constancia de prácticas</title><style>*{box-sizing:border-box}body{margin:0;background:#eee;font-family:Arial;color:#171717}.sheet{width:210mm;min-height:297mm;margin:0 auto;background:#fff;padding:18mm 18mm 16mm;position:relative;border-top:5px solid #b98727}.head{display:flex;align-items:center;justify-content:space-between;border-bottom:2px solid #b98727;padding-bottom:10mm}.head img{width:52mm;max-height:25mm;object-fit:contain}.brand{text-align:right;font-size:10px;letter-spacing:.14em;color:#777}.title{text-align:center;font-family:Georgia,serif;font-size:23px;margin:18mm 0 13mm}.body p{font-size:12.5px;line-height:1.8;text-align:justify;margin:0 0 7mm}.date{text-align:right!important;margin-top:10mm!important}.sign{width:90mm;margin:28mm auto 0;text-align:center;border-top:1px solid #222;padding-top:3mm;font-size:12px;line-height:1.5}.foot{position:absolute;left:18mm;right:18mm;bottom:13mm;border-top:1px solid #d6c29b;padding-top:4mm;text-align:center;font-size:9px;color:#666;letter-spacing:.05em}.accent{position:absolute;right:0;top:0;width:24mm;height:24mm;background:#eee3cf;clip-path:polygon(100% 0,100% 100%,0 0)}@media print{@page{size:A4;margin:0}body{background:#fff}.sheet{margin:0;width:210mm;height:297mm;min-height:297mm;page-break-after:avoid}}</style></head><body><div class="sheet"><div class="accent"></div><div class="head"><img src="${logo}"><div class="brand">ARQUITECTURA · CONSTRUCCIÓN<br>INTERIORES · INMOBILIARIA</div></div><h1 class="title">CONSTANCIA DE PRÁCTICAS PREPROFESIONALES</h1><div class="body"><p>El/La que suscribe, <b>${supName}</b>${supMeta?`, ${supMeta.replace('<br>',', ')}`:''}, en representación de <b>${esc(company.legal)}</b>, con RUC N.° <b>${esc(company.ruc)}</b>, hace constar que <b>${esc(x.name)}</b>, identificado(a) con DNI/CE N.° <b>${esc(x.dni||'________')}</b>, estudiante de ${x.faculty?`la Facultad de <b>${esc(x.faculty)}</b>, `:''}${x.career?`la carrera/escuela profesional de <b>${esc(x.career)}</b> `:''}de <b>${esc(x.school||'________________')}</b>, realizó sus prácticas preprofesionales en nuestra empresa, bajo supervisión, desde el <b>${fmtDate(x.start)}</b> hasta el <b>${fmtDate(x.end)}</b>${x.hours?`, cumpliendo un total de <b>${esc(x.hours)} horas</b>`:''}.</p><p>Durante dicho periodo, ${esc(x.name)} desarrolló sus prácticas satisfactoriamente, demostrando eficiencia, puntualidad, responsabilidad y adecuada formación académica en las actividades que le fueron encomendadas.</p><p>Se expide la presente constancia a solicitud del/de la interesado(a), para los fines que considere convenientes.</p><p class="date">Huancayo, <b>${issued}</b></p></div><div class="sign"><b>${supName}</b><br>${supMeta}<br>${esc(company.legal)}<br>RUC ${esc(company.ruc)}</div><div class="foot">${esc(company.trade||'VISMO')} · ${esc(company.address||'Huancayo, Junín')} · Visión y modernidad para vivir mejor</div></div></body></html>`);w.document.close()});
// Replace legal company text at preview time, preserving old saved documents as snapshots when exported/printed.
document.getElementById('previewBtn')?.addEventListener('click',()=>setTimeout(()=>{document.querySelectorAll('.companyContractText').forEach(el=>el.innerHTML=`${esc(company.legal)}, con RUC N.° ${esc(company.ruc)}${company.address?`, domiciliada en ${esc(company.address)}`:''}`)},0),true);
loadCompany();renderPros();renderTemplates();renderInterns();
})();
